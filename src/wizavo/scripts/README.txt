
port.txt
    * About:
    Represents the port number shared by Python and C++ for
    TCP connection.
    * Note:
    Initial number must be between 49152 and 65535.

main.cpp
    $ ./wiz_client [speaker type]
    [speaker type] is "ftn" or "myt".

wizavo_server.py
    * Note:
    Need to rewrite the value of "portfile" to current directory.
